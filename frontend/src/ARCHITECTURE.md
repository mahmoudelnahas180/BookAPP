# هيكلة المشروع (Project Architecture)

يعتمد هذا المشروع على **Feature-Based Architecture** (الهيكلة القائمة على الميزات)، حيث يتم تجميع الملفات بناءً على الوظيفة (Books, Auth, Cart) وليس بناءً على نوع الملف فقط.

## 📂 شجرة الملفات (File Tree)

```text
src/
├── assets/          (الصور، الخطوط، والأيقونات)
├── components/      (مكونات واجهة عامة: Buttons, Inputs, Modals)
├── config/          (الإعدادات العامة: Constants, Themes, Routes)
├── features/        (قلب المشروع - كل ميزة رئيسية في مجلد مستقل)
│   ├── auth/        (Login, Register, ProtectedRoute)
│   ├── books/       (BookCard, BookList, Filter)
│   ├── cart/        (CartItem, Checkout)
│   └── admin/       (DashboardCharts, UserTable)
├── hooks/           (Custom Hooks: useDebounce, useLocalStorage)
├── layouts/         (قوالب الصفحات: AdminLayout, UserLayout, AuthLayout)
├── pages/           (تجميع المكونات لتكوين صفحات كاملة)
├── services/        (الربط مع السيرفر: API calls, Axios config)
└── utils/           (دوال مساعدة: Formatting, Validation)
```

---

## 📖 شرح تفصيلي للمجلدات

هذا الهيكل التنظيمي (Folder Structure) يُعتبر من أفضل الممارسات حالياً في تطوير تطبيقات React الكبيرة والمتوسطة. بدلاً من تجميع الملفات حسب نوعها، نحن هنا نجمعها حسب الوظيفة (Business Logic).

### 1. `src/assets/` (الأصول والملفات الثابتة)
هنا نضع الملفات التي لا تحتوي على كود برمجي (Logic) ولكنها ضرورية للمظهر.
*   **أمثلة:** الشعارات (Logos)، الخطوط العربية (Cairo, Tajawal)، الصور التوضيحية، والأيقونات الخاصة.

### 2. `src/components/` (المكونات العامة - Shared UI)
هنا نضع المكونات "الغبية" (Dumb Components) التي لا تعرف شيئاً عن منطق العمل، بل هي مجرد عناصر واجهة قابلة لإعادة الاستخدام.
*   **أمثلة:** `Button.jsx`, `Input.jsx`, `Modal.jsx`, `Spinner.jsx`.
*   **الهدف:** توحيد التصميم في كامل الموقع وتجنب تكرار الكود.

### 3. `src/config/` (الإعدادات العامة)
ملفات الضبط التي تتحكم في سلوك التطبيق بالكامل.
*   **أمثلة:** الثوابت (`constants.js`) مثل `API_BASE_URL` أو إعدادات الألوان والخطوط (`theme.js`).

### 4. `src/features/` (قلب المشروع - The Core) 🔥
هذا أهم مجلد. يتم تقسيم المشروع هنا إلى "ميزات" (Features). كل مجلد يحتوي على كل ما يخص الميزة:
*   **`auth/`**: كل ما يخص تسجيل الدخول، التسجيل، وحماية المسارات.
*   **`books/`**: عرض الكتب، قائمة الكتب، وفلاتر البحث.
*   **`cart/`**: عناصر السلة، منطق الإضافة والحذف، ونموذج الدفع.
*   **`admin/`**: لوحة التحكم، الجداول، والرسوم البيانية للمبيعات.

### 5. `src/hooks/` (Custom Hooks)
أكواد JavaScript (Logic) قابلة لإعادة الاستخدام ولكنها ليست مكونات مرئية.
*   **أمثلة:** `useLocalStorage.js` لحفظ البيانات في المتصفح، أو `useDebounce.js` لتحسين أداء البحث.

### 6. `src/layouts/` (تخطيط الصفحات)
القوالب الأساسية التي تغلف الصفحات وتحدد مكان الهيدر والفوتر.
*   **أمثلة:** `UserLayout.jsx` (للمستخدم العادي)، `AdminLayout.jsx` (للأدمن)، و `AuthLayout.jsx` (لصفحات الدخول).

### 7. `src/pages/` (الصفحات - Routes)
هنا يتم تجميع المكونات من المجلدات السابقة لتكوين صفحة كاملة مرتبطة بمسار (URL) معين.
*   **أمثلة:** `HomePage.jsx`, `LoginPage.jsx`, `BookDetailsPage.jsx`.

### 8. `src/services/` (الربط مع الباك إند - API)
المسؤول عن التحدث مع السيرفر باستخدام Axios أو Fetch.
*   **الهدف:** فصل كود جلب البيانات عن كود عرضها (Separation of Concerns).

### 9. `src/utils/` (الأدوات المساعدة - Helpers)
دوال JavaScript بسيطة تقوم بمهام حسابية أو تنسيقية.
*   **أمثلة:** `formatCurrency.js` لتنسيق الأسعار، أو `validation.js` للتأكد من صحة البيانات.

---

## 💡 لماذا هذا الهيكل ممتاز؟
1.  **سهولة الصيانة**: الوصول السريع للملفات بناءً على وظيفتها.
2.  **العمل الجماعي**: تقليل التداخل بين المبرمجين عند العمل على ميزات مختلفة.
3.  **قابلة التوسع (Scalability)**: إضافة ميزات جديدة بسهولة دون التأثير على استقرار النظام الحالي.
